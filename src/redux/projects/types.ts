export interface projectMember {
    image: string;
    name: string;
}

export interface project {
    id: string;
    projectName: string;
    projectDescription: string;
    projectOwner: string;
}